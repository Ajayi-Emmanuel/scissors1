<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/form.css">
    <title>SCISSORS || Sign Up</title>
</head>
<body>
    <div class="container">
        <h1>Scissors</h1>
        <form class="signup" method="post">
            <h2>Sign Up</h2>
            <div class="form-label">
                <input type="email" id="email" required name="email" placeholder="Enter Email"/>
            </div>
            <div class="form-label">
                <input type="password" id="pwd" required name="password" placeholder="Enter Password"/>
            </div>
            <div class="form-label">
                <button class="btn" type="submit">Submit</button>
            </div>
            <p class="form-label">Have an Account? <a href="/login">Login</a> Here</p>

            <% if(error){ %>
                <div class="error-message">
                    <p><%= error %></p>
                </div>
            <% } %> 
    
        </form>
    </div>
</body>
</html>